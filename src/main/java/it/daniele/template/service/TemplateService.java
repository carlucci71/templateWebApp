package it.daniele.template.service;

import org.springframework.stereotype.Service;



@Service
public class TemplateService {
/*
	@Autowired
	private QuizFactory quizFactory;

	@Autowired
	QuizRepository quizRepository;

	@Autowired
	LogFactory logFactory;
	
	@Autowired
	LogRepository logRepository;
	@Transactional
	public QuizDTO insert(InputQuizDTO inputQuizDTO) {
		Long id = quizRepository.inserisci(inputQuizDTO);
		return quizFactory.toDTO(quizRepository.findById(id).get());
	}
	*/
	
}
